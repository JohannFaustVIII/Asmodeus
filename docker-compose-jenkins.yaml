version: "3.9"

services:
  jenkins:
    container_name: jenkins
    image: jenkins/jenkins:lts-jdk11
    ports:
      - "8080:8080"
      - "50000:50000"
    volumes:
      - type: bind
        source: ./jenkins
        target: /var/jenkins_home